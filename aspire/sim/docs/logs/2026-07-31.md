# 2026-07-31

## Canonical BEHAVIOR-1K ASPIRE protocol documentation

- Made `.claude/behavior/fix-loop/` the single source of truth for Soda Can and
  Radio campaigns; there is no separate protocol folder.
- Defined development seeds 26-35 as the skill-acquisition partition and
  held-out seeds 1-25 as isolated fresh-agent adaptation episodes.
- Clarified that the freeze boundary covers the learned skill library, config,
  prompts, model, and budgets—not a generated policy.
- Required a fresh policy and fresh Claude Code context for every held-out seed,
  while preserving block-by-block debugging within that seed.
- Required replay-only execution so external Claude Code is the sole model in
  the loop; the shared built-in ASPIRE `REGENERATE`/`FINISH` implementation was
  left intact for other experiments.
- Added concise campaign-state, clean-slate, coordinator, Stage 1, and Stage 2
  templates for reproducible and resumable execution.
- Corrected BEHAVIOR Tyro command examples to use the bare `--record-video`
  boolean flag.
- No experiment, service, simulator, or real hardware was started. Validation
  for this change is limited to documentation consistency and link checks.
